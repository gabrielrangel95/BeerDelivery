import { metrics } from './index';

// components
  // button
export const BUTTON_HEIGHT = 42;
export const BUTTON_WIDTH = metrics.SCREEN_WIDTH * 0.4;
  // product card
export const PRODUCT_CARD_WIDTH =  metrics.SCREEN_WIDTH * 0.4;
export const PRODUCT_CARD_HEIGHT = PRODUCT_CARD_WIDTH * 1.4;
export const PRODUCT_CARD_ACTION_BUTTON_SIZE = 28;

// products screen
export const CATEGORIES_CONTAINER_HEIGHT = metrics.SCREEN_HEIGHT * 0.05;
